version: '3.8'
services:
  reverse-proxy:
    image: nginx:alpine
    ports:
      - "443:443"
    volumes:
      - ./certs:/certs:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on: []
    restart: unless-stopped
    # 用作本地 TLS 终端，反代到主机的 CIAM :3001（需本机先启动 npm run dev）

